function [glmModel] = designMatrixBuilder_whisking(glmModel,glmnetOpt,selectedFeatures)

%selectedFeatures is a vector with values corresponding to the values in
%the DmatFields 

for i = 1:length(glmModel)
    
    
end